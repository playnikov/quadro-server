ktor {
    application {
        modules = [ com.quadro.gateway.ApplicationKt.module ]
    }
    deployment {
        port = 8080
    }
}

rateLimiting {
  enabled = true
  defaultLimit = 100
  defaultWindow = 60s
}

jwt {
  issuer = "quadro"
  audience = "quadro-api"
  realm = "quadro"
}